Работаем в репозитории:
onliner7-collab/kuhni-na-zakaz

Ветка:
work

Контекст по текущему состоянию:
- `PriceRule` уже существует и используется для `/calculator` и `/admin/prices`
- в `HANDOFF.md` сейчас прямо отмечено, что `/prices` ещё не доведена: страница функциональна, но использует hardcoded `SEGMENTS`, тогда как `PriceRule × 34` уже питают `/calculator` :contentReference[oaicite:0]{index=0}
- цель этого этапа: довести именно `/prices`, не трогая другие большие подсистемы

Главное правило:
- один этап = один commit
- не смешивать с blog / email / images / docs cleanup других этапов
- не делать посторонний refactor

==================================================
ЭТАП 5 — PRICES PAGE COMPLETION
==================================================

Цель:
довести публичную страницу `/prices` до состояния, где она опирается на реальные данные проекта, а не на разрозненный хардкод, и согласована с `/admin/prices` и `PriceRule`.

Что нужно проверить:
- `app/prices/page.tsx`
- все компоненты, которые используются на `/prices`
- `PriceRule`
- `/admin/prices`
- `/kapi/calculator`
- существующие сиды / данные для цен
- metadata страницы `/prices`

Что нужно сделать:
1. Найти, где именно в `/prices` сейчас используются hardcoded `SEGMENTS` или аналогичные статические ценовые блоки.
2. Перевести `/prices` на чтение из БД там, где это архитектурно уже поддерживается через `PriceRule`.
3. Если для публичной страницы не хватает данных:
   - добавить минимально достаточный seed/инициализацию для `PriceRule` или связанных публичных отображений
   - но не дублировать данные в новой сущности без необходимости
4. Согласовать публичную `/prices` с `/admin/prices`:
   - admin редактирует те же данные, которые реально влияют на публичную страницу, где это уместно
   - не оставлять две расходящиеся системы цен
5. Проверить тексты и metadata:
   - без неподтверждаемых обещаний
   - без старого минского позиционирования в глобальном смысле
   - если страница общая, она должна соответствовать бренду “по Беларуси”
6. Сохранить бизнес-логику калькулятора:
   - не ломать `/kapi/calculator`
   - не переписывать формулу без необходимости
7. Если полностью убрать хардкод за один этап нельзя:
   - оставить только минимально оправданный остаточный fallback
   - явно задокументировать, что осталось fallback и почему

==================================================
ЧТО ЗАПРЕЩЕНО
==================================================

- не делать email notifications
- не делать image upload/storage
- не трогать blog
- не трогать regional pages
- не делать unrelated refactor
- не менять auth
- не менять StaticPage CMS
- не возвращать `.next/`
- не писать в docs неподтверждаемые утверждения
- не делать несколько этапов в одном commit

==================================================
КРИТЕРИЙ ГОТОВНОСТИ
==================================================

Этап считается завершённым только если:

1. `/prices` больше не зависит от бессистемного hardcoded набора цен там, где уже можно использовать DB-driven данные.
2. Публичная страница цен логически согласована с `PriceRule` и `/admin/prices`.
3. Калькулятор не сломан.
4. Metadata и тексты страницы аккуратны и соответствуют бренду.
5. Изменения ограничены именно задачей цен.
6. `project-docs/CHANGELOG.md` и `project-docs/HANDOFF.md` обновлены только фактами.

==================================================
НУЖНЫЕ СКИЛЛЫ
==================================================

- Next.js App Router
- Prisma
- SSR pages
- DB-driven UI
- SEO / metadata hygiene
- content consistency
- git hygiene
- аккуратный рефакторинг без поломки формулы калькулятора

==================================================
ЧТО СДАТЬ
==================================================

После завершения:
1. Список изменённых файлов.
2. Кратко по каждому — что изменено.
3. Отдельно указать:
   - что на `/prices` было hardcoded раньше
   - что стало DB-driven теперь
   - что, если осталось, всё ещё fallback и почему
4. Подтвердить, что `/admin/prices` и `/kapi/calculator` не сломаны логически.
5. SHA финального commit.

==================================================
ФОРМАТ РАБОТЫ
==================================================

- Сначала проанализируй `/prices`, `PriceRule`, `/admin/prices`, `/kapi/calculator`
- Потом внеси только необходимые изменения
- Затем сделай один аккуратный commit
- В ответе дай только фактический отчёт и SHA коммита