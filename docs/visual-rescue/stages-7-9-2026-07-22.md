# Visual rescue — этапы 7–9

Дата: 2026-07-22

Scope: `/catalog/kuhni-s-ostrovom`, `/catalog/malenkie-kuhni`, `/catalog/kuhni-do-potolka`

Локальный статус: `VISUAL_ACCEPTED_LOCAL`; production status устанавливается только после deploy smoke.

## KEEP / ADAPT / REPLACE / MOVE / REMOVE

- `KEEP`: canonical URL, metadata/schema path, Server Component shell, ExploreContext, ContactForm, RelatedExplorationRail, global Dock и нижние server-rendered ссылки.
- `ADAPT`: `LayoutVisualExplorer` получил route-specific series и ровно один eager LCP-кадр; остальные состояния монтируются после выбора.
- `REPLACE`: legacy generic media pair и text-only result заменены шестью visual states на каждом route.
- `MOVE`: объяснение оставлено после visual journey в `<details>`.
- `REMOVE`: ранний длинный fallback-copy не выводится для route с принятой visual series.

## Media

| Route | SeriesId | Masters | Meaningful visual changes | WebP | AVIF |
| --- | --- | ---: | --- | --- | --- |
| `/catalog/kuhni-s-ostrovom` | `LAYOUT-ISLAND-2026-07-22` | 6 | проход, подготовка, хранение, посадка, второй ракурс | 40–55 КБ | 27–39 КБ |
| `/catalog/malenkie-kuhni` | `LAYOUT-SMALL-2026-07-22` | 6 | столешница, хранение, техника, проход, компромисс | 33–39 КБ | 22–27 КБ |
| `/catalog/kuhni-do-potolka` | `LAYOUT-CEILING-2026-07-22` | 6 | ежедневная зона, верхний ярус, сезонное хранение, доступ, техзазор | 20–32 КБ | 12–22 КБ |

Все 18 PNG masters сохранены внутри project public media root и не используются как visible `src`. Contact sheets находятся рядом с сериями. Встроенный Codex/OpenAI `imagegen` использован для всех новых masters. Provenance: `ai_concept`; rights: `internal_generation`; все captions явно отделяют визуализацию от реализованного объекта.

## Mobile visual gate

На 390×844 для всех трёх routes: начало изображения Y=426 px, высота видимой visual зоны 445 px, первая кнопка Y=931 px — в начале второго viewport. До visual action присутствуют H1, один короткий вопрос и visual; длинное объяснение раскрывается после серии. Две text-only sections подряд отсутствуют.

Initial delivery одинаково для трёх routes: один image request, только active AVIF 1200×800; `naturalWidth=1200`; остальные пять кадров не запрашиваются до intent. Route bundle build: `/catalog/[slug]` First Load JS 189 КБ; локальный resource transfer при 390 px — около 256 КБ scripts. Наблюдение Chromium production build: LCP 184–196 мс, CLS 0.0002, overflow 0. Это локальная лабораторная проверка, не field CWV и не гарантия production INP.

## Accessibility / responsive / SEO

- Playwright: 4/4 PASS; widths 360/390/412/768/1440.
- Все 18 state buttons keyboard-reachable, `aria-pressed`, visible focus, высота не меньше 44 px; selected state имеет icon + `aria-pressed`, не только цвет.
- `prefers-reduced-motion` не получает обязательную анимацию; autoplay отсутствует.
- На targets: H1=1, self canonical, русские alt/caption, images load, no horizontal overflow.
- Protected regression: `/`, `/design-proekt-kuhni`, `/locations/minskaya-oblast`, `/locations/minsk`, `/materials/furnitura` — H1=1, overflow 0, broken images 0.
- Sitemap: 112 URL; новые facet/query URL не создавались.

## Проверки и evidence

- `pnpm.cmd run typecheck` — PASS.
- `pnpm.cmd run sitemap:check` — PASS, 112 URL, штатный static fallback.
- `pnpm.cmd run build` — PASS, 127 static pages; ожидаемые Prisma fallback warnings при недоступной локальной `127.0.0.1:5434`.
- `pnpm.cmd exec playwright test -c playwright.stages-7-9.config.ts tests/visual-rescue/stages-7-9.spec.ts` — 4/4 PASS.
- Screenshots: `artifacts/visual-rescue/stages-7-9/*-390-{initial,after-first-action,visual-result}.png`.
- Videos/report: `artifacts/visual-rescue/stages-7-9/playwright-results/`, `playwright-report.json`.
- Contact sheets: `/media/visual-rescue/{kuhni-s-ostrovom,malenkie-kuhni,kuhni-do-potolka}/contact-sheet.webp`.

## Deploy / rollback

До runtime commit и production smoke локальный статус не повышать. Standard deploy: push `work`, затем `bash /var/www/kuhni-na-zakaz/deploy/scripts/update-production.sh work`. Rollback: отдельный `git revert <stage-7-9-commit>`, push/deploy и повторный smoke трёх targets + пяти protected routes. Prisma/schema/import rollback не требуется.
