# Visual rescue — этапы 4, 5 и 6

Дата: 2026-07-22. Ветка: `work`.

## Scope и diff-аудит

| Этап | Route | KEEP | ADAPT / REPLACE | MOVE / REMOVE |
| --- | --- | --- | --- | --- |
| 4 | `/materials/mdf-fasady` | canonical, metadata, schema, ограничения, форма, переходы, 4 принятых media | hero и comparator преобразованы в одну шестикадровую visual journey | длинное объяснение осталось после интеракции; дублирующий hero удалён |
| 5 | `/catalog/pryamye-kuhni` | canonical, server answer, форма, ExploreContext, переходы | двухкадровый generic media block заменён на 6 route-specific состояний длины/ракурса | подробное объяснение перенесено в `<details>` после visual action |
| 6 | `/catalog/p-obraznye-kuhni` | canonical, server answer, форма, ExploreContext, переходы | двухкадровый generic media block заменён на 6 состояний прохода/техники/L-сравнения | подробное объяснение перенесено в `<details>` после visual action |

`/materials/furnitura` и остальные четыре protected routes page-specific не менялись.

## Media

- Новые masters: 14 PNG, созданные встроенным Codex/OpenAI `imagegen`.
- Принятые existing masters: 4 для МДФ.
- Delivery: 14 WebP + 14 AVIF; видимый UI использует AVIF/WebP, PNG остаются sources.
- WebP: 17.5–50.7 КБ; AVIF: 5.1–26.8 КБ; 1200×800.
- Series: `MF-SURFACE-2026-07-22`, `LAYOUT-LINE-2026-07-22`, `LAYOUT-U-2026-07-22`.
- Provenance: `ai_concept`; rights: `internal_generation`; подпись не выдаёт изображения за реализованные проекты.
- Briefs: `design/18-stages-4-6-media-briefs.md`.
- Manifest: `content/media/visual-rescue-stages-4-6-2026-07-22.json`.

## Visual acceptance

| Route | Masters | Meaningful changes | First action | Copy before action | Text-only sections |
| --- | ---: | ---: | --- | --- | ---: |
| `/materials/mdf-fasady` | 6 | 5 | control сразу после portrait visual, не позже второго viewport | H1 + 2 коротких предложения | нет двух больших подряд |
| `/catalog/pryamye-kuhni` | 6 | 5 | control сразу после portrait visual, не позже второго viewport | H1 + один вопрос | нет двух больших подряд |
| `/catalog/p-obraznye-kuhni` | 6 | 5 | control сразу после portrait visual, не позже второго viewport | H1 + один вопрос | нет двух больших подряд |

На 390×844 активный visual имеет портретный слот 4:5. Остальные кадры меняют `currentSrc` после явного выбора и не монтируются как набор скрытых изображений.

## QA

- Typecheck: PASS.
- Assets validation: PASS, 0 errors; старые 7 aspect-ratio warnings не относятся к этому batch.
- Sitemap: PASS, 112 URLs, static fallback при недоступной local DB.
- Production build: PASS, 127 static pages; local PostgreSQL `127.0.0.1:5434` недоступен, fallback path отработал.
- Playwright: 4/4 PASS; target matrix 360/390/412/768/1440, protected routes 5/5 на 390.
- H1: 1; self canonical: PASS; `naturalWidth > 0`; touch targets ≥44 px; overflow 0; visual `currentSrc` меняется.
- Local 390 performance observation: LCP 284–304 мс в dev, CLS 0.0002, visual swap 65–113 мс. Это laboratory observation, не field CWV.
- Initial route visual requests: 1 для обеих planning routes; 2 для MDF (active visual + lower lazy cutaway); полная series не грузится до intent.
- First Load JS из production build: shared 102 КБ; `/catalog/[slug]` 189 КБ, `/materials/mdf-fasady` 157 КБ.

## Evidence

- Screenshots: `artifacts/visual-rescue/stages-4-6/*-390-initial.png`, `*-after-first-action.png`, `*-visual-result.png`.
- Contact sheets: `artifacts/visual-rescue/stages-4-6/*-contact-sheet.webp`.
- Videos: `artifacts/visual-rescue/stages-4-6/playwright-results/*/video.webm`.
- Metrics: `artifacts/visual-rescue/stages-4-6/performance-evidence.json`.
- Playwright report: `artifacts/visual-rescue/stages-4-6/playwright-report.json`.

## Commits и rollback

- Этап 4: `94b69e5` — `git revert 94b69e5`.
- Этап 5: `39c900f` — `git revert 39c900f`.
- Этап 6: `531c2a2` — `git revert 531c2a2`.
- Evidence/registry commit: `26fe334`.
- Runtime rollback: revert нужных commits, push `work`, затем `bash /var/www/kuhni-na-zakaz/deploy/scripts/update-production.sh work`, после чего повторить smoke трёх routes и пяти protected routes.

## Production

Статус: `VISUAL_ACCEPTED / PRODUCTION_PASS`.

- Push: `work` → `origin/work`, `ba806a2..26fe334`.
- Deploy: `ssh -i C:\Users\User\.ssh\timeweb_kuhni_ed25519 root@5.42.108.140 "bash /var/www/kuhni-na-zakaz/deploy/scripts/update-production.sh work"` — exit 0; production build PASS; service `kuhni-na-zakaz` active.
- Production target smoke: 3/3 HTTP 200, self-canonical, H1=1, active image HTTP 200, `naturalWidth=1200`, visual change PASS, overflow 0.
- Protected production smoke: 5/5 HTTP 200, H1=1, broken images 0, overflow 0.
- `/sitemap.xml` и `/robots.txt`: HTTP 200.
- Production evidence: `artifacts/visual-rescue/stages-4-6/production-smoke.json` и `*-390-production-result.png`.
- Следующий разрешённый этап: 7 из `design/17-visual-rescue-single-file-tz.md`.
