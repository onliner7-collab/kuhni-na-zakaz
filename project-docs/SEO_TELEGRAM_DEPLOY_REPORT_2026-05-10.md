# Отчет: SEO, Telegram и деплой

Дата: 2026-05-10

## Что проверено

- Основное приложение `artifacts/kuhni-na-zakaz`.
- Админ-панель `/admin`.
- Страница Telegram-уведомлений `/admin/notifications`.
- Публичная отправка заявки через `/kapi/leads`.
- `robots.txt` и `sitemap.xml`.
- TypeScript и production-сборка.

## Что исправлено

- `next.config.ts`: отключено скрытие TypeScript-ошибок во время production build.
- `middleware.ts`: канонический host теперь берется из `NEXT_PUBLIC_SITE_URL`, а не из жестко заданного домена.
- `lib/schema-org.tsx`: JSON-LD URL строятся через `getSiteUrl()`, чтобы schema.org, sitemap и canonical не расходились по доменам.
- `lib/telegram.ts`: Telegram token и Chat ID нормализуются через `trim()`, а ошибки Telegram API переводятся в понятные сообщения для админки.

## Результаты проверок

- `pnpm --filter @workspace/kuhni-na-zakaz run typecheck` - успешно.
- `pnpm --filter @workspace/kuhni-na-zakaz run build` - успешно.
- `/sitemap.xml` - 51 URL, double slash не найден.
- `/robots.txt` - доступен, `Sitemap` указан.
- Админка - вход и раздел Telegram открываются.
- Лид-форма - тестовая заявка создалась через API, затем тестовая запись удалена из локальной БД.

## Telegram

Для доставки уведомлений в Telegram в админке должны быть заполнены:

- токен бота из `@BotFather`;
- хотя бы один активный получатель с корректным `Chat ID`;
- пользователь или группа должны сначала написать боту или добавить бота в группу.

Типовые ошибки теперь показываются понятнее:

- неверный токен;
- `Chat ID` не найден;
- пользователь заблокировал бота;
- Telegram не смог разобрать HTML-сообщение.

## Что нужно сделать после деплоя

- Открыть `/admin/notifications`.
- Повторно отправить тестовое сообщение.
- Если ошибка останется, проверить точный текст ошибки в админке.
- Проверить новую заявку с публичной формы и убедиться, что сообщение приходит всем активным получателям.

## SEO-дальше

- Убедиться, что `NEXT_PUBLIC_SITE_URL` на сервере соответствует реальному продакшен-домену.
- Отправить актуальный sitemap в Google Search Console и Яндекс.Вебмастер.
- Проверить ключевые страницы в инструментах индексации Google и Яндекса.
- Добавить реальные соцссылки и контакты в настройки сайта для расширения `Organization` / `LocalBusiness` разметки.
